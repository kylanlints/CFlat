{
    int flag = 0;
    for (unsigned long i = 0; 1; i = i + 1) {
        flag = 1;
        for (long j = 2; (unsigned long) j <= i/2; j = j + 1) {
            if (i - (j * (i / j)) == 0) {
                flag = 0;
                break;
            }
        }

        if (flag) {
            int REPLACE = 12345;
        }
    }
    do {
        int x = 5;
    } while (x == 6);
    i = i - 1;
    exit(i);
}

