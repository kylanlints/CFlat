#include <stdio.h>

int hi2 (float a2) {
    float b = a2 + 2.0;
    return b;
}

int main(int argc, char* argv[]) {
    int b = 567;
    hi2((float) b);
    
    int x = 5;
    int y = 10;
    int z = x + 23901238;
    scanf("%d", &x);
    scanf("%d", &y);
    square(3);
    printf("%d", z*x);
}

/* Type your code here, or load an example. */
int square(int num) {
    double num2 = num * (double) num;
    float num3 = num * num;
    int a = 3;
        int b = 4;
        int c = 5;
        int d = a / 5 + b * 8 - c * 5 * c - 5 * a * a;
        int e = 5;
        int f = 5;
        int g = 5;
        int h = 5;
        int j = 5;
        int k = 5;
        int l = 5;
        int i = 5;
        int u = 5;
        int y = 5;
        int t = 5;
        int r = 5;
        int w = 5;
        int q = 5;
        int s = 5;
        int dd = 5;
        int aa = 6;
        int bb = 5;
        int ccc = 5;
        int ddd = 5;
        int ee = 5;
        int ff = 5;
        int gg = 5;
        int hh = 5;
        int ii = 5;
        int jj = 5;
        int kk = 5;
        int ll = 5;
        int mm = 5;
        scanf("%d", &a);
        scanf("%d", &b);
        scanf("%d", &c);
        scanf("%d", &d);
        scanf("%d", &e);
        scanf("%d", &f);
        scanf("%d", &g);
        scanf("%d", &h);
        scanf("%d", &j);
        scanf("%d", &k);
        scanf("%d", &i);
        scanf("%d", &l);
        scanf("%d", &u);
        scanf("%d", &y);
        scanf("%d", &t);
        scanf("%d", &r);
        scanf("%d", &w);
        scanf("%d", &q);
        scanf("%d", &s);
        scanf("%d", &dd);
        scanf("%d", &aa);
        scanf("%d", &bb);
        scanf("%d", &ccc);
        scanf("%d", &ddd);
        scanf("%d", &ee);
        scanf("%d", &ff);
        scanf("%d", &gg);
        scanf("%d", &hh);
        int cc = (a + (b - c + d / (e*e / (f*f / (g*f / (h*h / (j*j / (k*k / (l*l / (i*i / (u*u / (y*y / (t*t / (r*r / (w*w / (q*q / (s / (dd / (aa / (bb / (ccc / (ddd / (ee / (ff / (gg /  (hh / (ii / (jj / (kk / ((ll * kk) / (mm)))))))))))))))))))))))))))))));
    return 3/(num*cc*3);
}
